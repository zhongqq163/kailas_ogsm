# 开发记录

更新时间：2026-06-28

## 记录

### 2026-06-22

- 目标：初始化 AGSM 网页版开发项目。
- 改动：创建 `Category_12_IT/IT_Project_01_AGSM网页版开发`，复制项目模板，补充项目控制台和初版需求记录。
- 验证：确认项目目录和模板文档已存在。
- 下一步：补充业务需求、页面范围、技术栈和验收标准。

### 2026-06-22

- 目标：基于部门 OGSM Excel 创建网页第一版。
- 改动：新增 `web/index.html`、`web/styles.css`、`web/app.js`，实现本地可编辑 AGSM 管理台。
- 数据：从 `IT数据应用支持组_钟启庆_OGSM_2026V2.0.xlsx` 提取 Object 和 4 条 Goal/Strategy/Measure/Tactics。
- 功能：搜索、状态筛选、负责人筛选、新增、编辑、删除、进度维护、自动保存、JSON 导入导出。
- 下一步：验证页面交互；确认是否进入后端、数据库、权限和 Excel 导入能力开发。

### 2026-06-23

- 目标：按反馈拆分维护入口、补充领导查看页面、优化视觉。
- 改动：重构为三入口：领导看板、个人展示、维护中心。
- 领导看板：整体进度环、成员进度、风险关注、目标推进明细。
- 个人展示：按负责人汇总目标、平均进度、风险项和 OGSM 展示卡。
- 维护中心：保留编辑、新增、删除、筛选、导入导出，改动后同步到看板和个人展示。
- 验证：语法检查通过；本地服务 `http://127.0.0.1:8088/` 可访问；页签切换和维护联动已验证。

### 2026-06-23

- 目标：增加账号权限原型。
- 改动：新增登录页、右上角账号状态、退出登录、管理员/个人角色。
- 演示账号：管理员 `admin / admin123`；个人账号以项目控制台当前记录为准。
- 权限：管理员查看全员；个人账号隐藏领导看板，只显示本人个人展示和本人维护列表；个人账号负责人字段锁定。
- 验证：登录页显示正常；管理员可看全员看板；个人账号只能进入个人展示/维护中心；退出后回到登录页。
- 注意：当前为前端权限原型，正式上线需要后端登录、接口鉴权和数据库权限控制。

### 2026-06-23

- 调整：管理员账号隐藏维护中心入口。
- 权限：管理员只保留领导看板和个人展示；个人账号保留个人展示和维护中心。

### 2026-06-23

- 目标：参考用户截图优化 OGSM 展示排版，并把目标进度改为行动计划完成率驱动。
- 改动：领导看板/个人展示改为 GOAL 展开式排版：蓝色目标标题条、策略条、指标/行动计划/进度/负责人表格。
- 改动：顶部统计改为 6 张卡片：成员数、目标数、策略数、衡量指标、已完成计划、未完成计划。
- 改动：维护中心新增行动计划完成情况勾选区，Tactics 按行自动拆成计划项。
- 规则：目标进度 = 已完成行动计划数 / 总行动计划数。
- 验证：管理员展示初始进度 0%、已完成计划 0、未完成计划 31；个人账号勾选 1 个计划后对应目标进度自动变为 13%。

### 2026-06-23

- 调整：普通账号个人展示隐藏成员选择栏，直接全宽显示本人 OGSM。
- 调整：维护中心新增“新增行动项”输入框和按钮，新增后自动追加到 Tactics 并生成勾选项。
- 调整：页面外层改为 100% 宽度，尽量铺满浏览器显示区域。
- 验证：新增行动项后计划数量增加，输入框清空，Tactics 自动包含新增内容。

### 2026-06-24

- 调整：个人账号进入个人展示时彻底隐藏成员筛选侧栏，账号即人员，不再选择成员。
- 改动：`memberPanel` 增加登录态控制和 CSS 兜底隐藏，个人展示主区域单列全宽。
- 验证：个人账号下成员侧栏 `hidden=true`，展示区为单列布局。

### 2026-06-24

- 调整：页面外层改为明确按浏览器视口撑满，取消居中固定工作台观感。
- 改动：补充 `html/body/app-shell/main` 全宽规则，取消 Object 和个人摘要窄宽限制，增加大屏布局列宽适配。
- 改动：CSS/JS 引用增加版本参数，降低浏览器旧缓存影响。
- 验证：浏览器中 `.app-shell` 宽度等于视口宽度，个人展示主区域铺满可用宽度。

### 2026-06-24

- 调整：顶部增加汇报信息：`OGSM 2026`、负责人、报告周期、部门、更新日期。
- 调整：新增个人账号 `lh / 123456`，成员名 `林荟`，并生成 4 条相似但已调整的 OGSM 目标。
- 调整：指标和行动改为结构化关联：每个指标下维护自己的行动项。
- 功能：指标可新增、编辑、删除；行动可新增、编辑、删除、勾选完成；序号按当前顺序自动生成。
- 验证：管理员可见 2 位成员 8 个目标；林荟个人账号可维护本人目标；新增指标、增加行动、勾选完成、自动进度计算通过。

### 2026-06-24

- 调整：使用浏览器验证流程对前端进行视觉美化。
- 改动：优化整体配色、汇报抬头、卡片阴影、看板指标、目标汇报表格、维护中心和指标行动编辑区样式。
- 改动：统一按钮和表单焦点样式，减少浏览器默认焦点框的突兀感。
- 验证：管理员看板和个人维护中心可正常访问，无横向溢出；CSS 新版本已加载。

### 2026-06-24

- 调整：顶部增加凯乐石风格文字标识。
- 改动：新增山峰图形角标和 `KAILAS / 凯乐石` 字样，使用 CSS 绘制，不依赖外部图片资源。
- 验证：标识正常显示，页面无横向溢出。

### 2026-06-24

- 调整：将凯乐石标识升级为图片版 logo。
- 改动：新增 `web/assets/kailas-logo.svg`，顶部抬头和登录页引用该 logo。
- 验证：logo 图片正常加载，页面无横向溢出。

### 2026-06-24

- 调整：剔除钟启庆账号和目标数据。
- 调整：新增成员陈远航、周若溪，账号分别为 `cyh / 123456`、`zrx / 123456`。
- 改动：种子数据改为按成员配置生成，当前林荟、陈远航、周若溪各 4 条 OGSM 目标。
- 改动：本地存储迁移会移除旧钟启庆目标，并补齐新成员目标；旧 `zqq` 登录态自动失效。

### 2026-06-24

- 调整：顶部“更新日期”改为读取所有目标的最新编辑日期。
- 规则：目标内容、指标、行动、勾选、增删等编辑会更新对应目标 `updatedAt`，顶部自动显示最大日期。
- 验证：JS 语法检查通过，页面服务正常返回。

### 2026-06-24

- 调整：本地静态服务改为内网可访问。
- 启动方式：`python -m http.server 8088 --bind 0.0.0.0 --directory web`
- 当前访问：本机 `http://127.0.0.1:8088/`；内网 `http://172.20.1.186:8088/`
- 验证：端口监听 `0.0.0.0:8088`，两个地址均返回 `200`。

### 2026-06-25

- 目标：准备部署到 Cloudflare Pages，并解决手机/电脑数据不同步问题。
- 改动：新增 `wrangler.toml`、`schema.sql`、`functions/api/state.js`。
- 改动：前端新增 `/api/state` 远程同步逻辑，优先读写 Cloudflare D1，API 不可用时回退 `localStorage`。
- 改动：新增 `DEPLOY_CLOUDFLARE.md`，记录 D1 创建、schema 初始化、Pages 绑定和发布步骤。
- 当前限制：账号仍为前端演示账号；多人同时编辑暂时是最后保存覆盖。

### 2026-06-27

- 目标：修正项目文件夹命名，使其符合 Workspace 规则。
- 依据：项目目录命名规则为 `Project_XX_项目名`，当前 `Project_01` 到 `Project_03` 已占用。
- 改动：项目路径从 `kailas_ogsm` 规范为 `Project_04_kailas_ogsm`。
- 验证：确认项目目录仍为 Git 仓库，项目文档中的当前路径已同步更新。

### 2026-06-27

- 目标：明确展示界面只读，编辑集中在维护中心。
- 改动：移除领导看板和个人展示中的行动完成勾选交互，改为只读状态标识。
- 保留：维护中心仍可编辑目标、指标、行动和完成状态。
- 验证：`node --check web/app.js` 通过；确认展示页编辑绑定已移除，维护中心编辑绑定仍保留。

### 2026-06-27

- 目标：修复维护中心左侧目标列表底部大面积留白。
- 原因：维护中心左右两列使用 CSS Grid，默认会拉伸子项；左侧 `goal-panel` 又和右侧编辑区共用 `min-height`，导致目标卡片少时左侧背景被撑高。
- 改动：维护中心 grid 改为顶部对齐，左侧目标列表按内容高度收缩，右侧编辑区保留最小高度。
- 验证：本地页面在 `1600x1476` 视口下检查，左侧列表底部只剩 1px 边框间隙，不再出现大片空白。

### 2026-06-27

- 目标：每条行动任务都维护负责人和协助人。
- 改动：行动数据结构新增 `owner` 和 `helper`；旧数据默认继承目标负责人和目标协助人。
- 改动：维护中心每条行动增加负责人、协助人输入框；展示页只读显示任务责任信息。
- 验证：`node --check web/app.js` 通过；确认任务负责人、协助人字段参与搜索和展示。

### 2026-06-27

- 目标：补充 9 名群成员候选名单，并支持任务负责人/协助人多选。
- 成员：罗飞、蓝浩、潘尔逊、彭泰请、秦钦勇、王汉钊、许森、钟启庆、邹一先。
- 改动：负责人和协助人输入框增加候选筛选；每个字段保留手动编辑，支持用顿号、逗号或空格填写多人。
- 验证：`node --check web/app.js` 通过；本地维护中心可看到候选成员选择器。

### 2026-06-27

- 目标：目标元信息前置，并补充开始日期、结束日期。
- 改动：将主导人、协助人、开始日期、结束日期、状态和进度移动到指标行动编辑区前面。
- 规则：主导人固定为当前登录账号持有者；目标协助人自动从行动任务负责人/协助人中汇总，并排除主导人。
- 改动：目标数据新增 `startDate` 和 `endDate`，旧 `dueDate` 作为结束日期兼容。
- 验证：`node --check web/app.js` 通过。

### 2026-06-27

- 目标：个人展示页增加一键回到顶部按钮。
- 改动：展示页右下角新增固定回顶按钮，仅在个人展示页显示，点击后平滑滚动到页面顶部。
- 验证：`node --check web/app.js` 通过。

### 2026-06-27

- 目标：日期输入框支持整框点击打开选择器。
- 改动：开始日期和结束日期输入框点击任意位置时调用浏览器原生日期选择器；不支持 `showPicker` 时保留普通聚焦。
- 验证：`node --check web/app.js` 通过。

### 2026-06-27

- 目标：新增行动协助人默认为空。
- 改动：新增行动时负责人默认继承目标主导人，协助人不再继承目标协助人，默认留空。
- 验证：`node --check web/app.js` 通过。

### 2026-06-27

- 目标：负责人/协助人改为框内下拉多选标签。
- 改动：点击负责人或协助人框内即可打开成员下拉；选中人员显示为标签，点击 `×` 可移除；输入文字后回车可手动追加。
- 验证：`node --check web/app.js` 通过。

### 2026-06-27

- 目标：使用雪山图片作为页面虚化背景。
- 改动：新增 `web/assets/kailas-mountain-bg.png`，通过 `body::before` 固定铺满并虚化，叠加浅色蒙层保证前景内容可读。
- 验证：`node --check web/app.js` 通过。

### 2026-06-27

- 目标：让个人展示页中间白色内容区透出背景图。
- 改动：降低展示 GOAL 卡片、策略条、表头和表格行的白色背景透明度，并保留毛玻璃模糊效果。
- 验证：`node --check web/app.js` 通过；本地页面计算样式确认卡片背景为 `rgba(255, 255, 255, 0.48)`，表格行为 `rgba(255, 255, 255, 0.3)`。

### 2026-06-27

- 目标：按高级前端标准重做页面视觉，并避免模板化 AI 风格。
- 方向：保留内部 OGSM 工作台的信息密度，取消雪山背景方案，恢复浅色工作台底色。
- 改动：重建 `web/styles.css` 视觉系统，统一色板、卡片、表格、按钮、维护表单、人员标签和响应式布局。
- 改动：顶部汇报信息去掉 emoji，改为克制的文本标签。
- 验证：`node --check web/app.js` 通过；Chrome 验证桌面/移动无横向溢出，领导看板回顶部按钮可回到 `scrollY = 0`。

### 2026-06-27

- 目标：在高级工作台基础上增强视觉层次，让页面更有高级感和装饰感。
- 改动：新增 premium polish 样式层，强化顶部细纹、高光、指标卡、GOAL 标题条、表格行、按钮和卡片阴影。
- 约束：不恢复雪山背景图，不改业务结构和交互逻辑。
- 验证：`node --check web/app.js` 通过；Chrome 验证桌面和移动端无横向溢出，新版本 CSS `20260627-premium-polish` 已加载。

### 2026-06-27

- 目标：继续加强高级感，让页面更有装饰性但不牺牲业务可读性。
- 改动：增强页面底纹、顶栏光束、面板高光线、指标卡纹理、目标标题条、按钮光泽和进度渐变。
- 约束：仍不恢复雪山背景图，不改数据和交互逻辑。
- 验证：`node --check web/app.js` 通过；本地 `8090` 返回新版资源 `20260627-premium-flair`；Chrome 验证桌面和移动端无横向溢出。

### 2026-06-27

- 目标：继续优化前端页面的可读性和维护体验。
- 改动：目标明细补充日期范围，移动端目标明细增加栏目标签；维护中心左侧列表与编辑头在桌面端保持稳定；强化输入框、下拉、人员标签、危险按钮和底部保存区状态。
- 约束：不改数据结构，不恢复背景图，不引入 React/shadcn 组件依赖。
- 验证：`node --check web/app.js` 通过；本地 `8090` 返回新版资源 `20260627-ui-refine`；Chrome 验证桌面、移动端和维护中心无横向溢出，移动端栏目标签与维护区 sticky 样式生效。

### 2026-06-27

- 目标：修复维护中心行动完成复选框顶部像缺口的问题。
- 原因：原生 checkbox 与全局输入框边框、padding、阴影叠加后，在当前缩放下出现边缘裁切感。
- 改动：为行动完成复选框和计划复选框增加独立自定义样式，固定尺寸、边框、选中态和焦点态。
- 验证：`node --check web/app.js` 通过；Chrome 验证维护中心复选框为 `20px`、四边 `2px`、`appearance: none`，新版资源 `20260627-checkbox-fix` 已加载。

### 2026-06-27

- 目标：让每个目标下的行动项更充实，展示页和维护页信息更饱满。
- 改动：12 个目标的行动计划从每个目标约 5 条扩充到约 8 条，补充验收、复盘、监控、培训、资产治理等更细动作。
- 改动：新增 `SEED_ACTION_VERSION` 迁移逻辑，会把新种子行动合并到已有本地/远程状态中，同时保留已有行动和完成状态。
- 约束：不覆盖用户已填写的行动内容、负责人、协助人和完成状态。
- 验证：`node --check web/app.js` 通过；12 个目标均为 8 条行动；Chrome 验证运行时总行动数为 96，维护中心林荟目标共 32 条行动，新版资源 `20260627-more-actions` 已加载。

### 2026-06-27

- 目标：突出顶部 OBJECT 总目标区域。
- 改动：将 OBJECT 文案区域强化为目标声明卡片，增加渐变底色、左侧强调线、胶囊标签、暗纹水印和更醒目的文字层级。
- 约束：不改业务数据和布局结构，右侧整体推进保持原位置。
- 验证：`node --check web/app.js` 通过；Chrome 验证新版资源 `20260627-object-focus` 已加载，OBJECT 区域卡片样式、水印、强调线和 900 字重生效，桌面/移动端无横向溢出。

### 2026-06-27

- 目标：修复目标明细中进度状态和负责人列没有按行动对齐的问题。
- 原因：状态列文本会换行变高，而负责人列原来按 chip 自身高度排列，两个独立列的行高不一致。
- 改动：统一状态行和负责人行的最小高度、间距和垂直居中方式，让每条行动的状态与负责人一一对齐。
- 验证：`node --check web/app.js` 通过；Chrome 验证新版资源 `20260627-owner-align` 已加载，状态行与负责人行前三条 action 的顶部差值均为 0。

### 2026-06-27

- 目标：修正顶部报表标题中的组织名称。
- 改动：将“应用支持一组 · OGSM进度汇报”改为“数据应用支持组 · OGSM进度汇报”。
- 改动：新增旧标题迁移逻辑，避免本地或远程状态里保存旧标题后刷新又显示回来。
- 验证：`node --check web/app.js` 通过；Chrome 验证旧 localStorage 标题会自动迁移为新标题，新版资源 `20260627-group-title` 已加载。

### 2026-06-27

- 目标：去掉目标明细表头第一列的井号。
- 改动：将动态报表表头中的 `#` 改为空白占位，保留下方指标序号展示和原有列宽对齐。
- 验证：`node --check web/app.js` 通过；本地页面加载新版脚本 `20260627-no-index-hash`。

### 2026-06-28

- 目标：去掉维护中心的“恢复 Excel 初版”入口。
- 改动：移除工具栏恢复按钮，并将对应事件绑定改为可选，避免按钮不存在时影响页面初始化。
- 验证：`node --check web/app.js` 通过；Chrome 登录维护中心确认按钮不存在，新版脚本 `20260628-remove-reset-btn` 已加载。

### 2026-06-28

- 目标：去掉顶部重复的部门名称展示。
- 改动：移除顶部小字 `IT 数据应用支持组`，将主标题从 `数据应用支持组 · OGSM进度汇报` 简化为 `OGSM进度汇报`，部门信息保留在下方信息胶囊中。
- 改动：新增旧标题迁移逻辑，把历史保存的组织名前缀标题自动迁移为新标题。
- 验证：`node --check web/app.js` 通过；Chrome 验证顶部标题为 `OGSM进度汇报`，页面不再显示顶部重复部门小字，新版脚本 `20260628-title-compact` 已加载。

### 2026-06-28

- 目标：维护中心增加一键回到顶部按钮。
- 改动：维护页新增右下角回顶按钮，复用领导看板和个人展示的视觉样式与平滑滚动逻辑。
- 改动：资源版本更新为 `20260628-maintain-top-btn-v2`，确保新版 CSS/JS 生效。
- 验证：`node --check web/app.js` 通过；Chrome 登录维护账号进入维护中心后确认按钮存在，点击后页面回到顶部。
